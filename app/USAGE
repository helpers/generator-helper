Description:
    Creates handlebars helper that can easily be used with Assemble.

Example:
    yo helper

    This will create:
        either gruntfile.js or gulpfile.js: Configuration for the task runner.
        bower.json: Front-end packages installed by bower.
        package.json: Development packages installed by npm.
        app/: Your application files.
        test/: Unit tests for your application.